<% if flash.to_h.any? %>
  <div class="container">
    <% flash.each do |message_type, message| %>
      <%= content_tag(:div, message, class: "alert alert-#{message_type == 'notice' ? 'success' : 'danger'} mb-4") do %>
        <%= message %>
      <% end %>
    <% end %>
  </div>
<% end %>