<% content_for :title, "お便り詳細" %>

<div class="container py-5">
  <%= render 'shared/flash_message' %>
  <div class="glass-morphism p-4">
    <div class="mb-4">
      <h1 class="text-gradient h3 mb-4">
        <i class="bi bi-envelope-open me-3"></i>
        お便り詳細
      </h1>
      
      <%= render @letter %>
    </div>

    <div class="d-flex gap-3">
      <%= link_to 'javascript:history.back()', class: "btn btn-outline-light rounded-pill px-4 hover-lift" do %>
        <i class="bi bi-arrow-left me-2"></i>
        戻る
      <% end %>
    </div>
  </div>
</div>