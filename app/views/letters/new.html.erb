<% content_for :title, "New Letter" %>

<h1>New Letter</h1>
<h2>To：<%= @letterbox.program.title %><br><%= @letterbox.title %></h2>
<%= render "form", letter: @letter, letterbox: @letterbox %>

<br>
