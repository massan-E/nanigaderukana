<div class="container py-5">
  <div class="d-flex justify-content-between align-items-center mb-5">
    <h2 class="display-6 fw-bold text-gradient mb-0">お便り箱一覧</h2>
    <%= link_to new_letterbox_path, class: "btn btn-primary rounded-pill px-4 hover-lift" do %>
      <i class="bi bi-plus-lg me-2"></i>新規お便り箱
    <% end %>
  </div>

  <div class="row g-4" id="letterboxes">
    <%= render @letterboxes %>
  </div>
</div>